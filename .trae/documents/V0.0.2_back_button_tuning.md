# V0.0.2 调优计划 — "返回上一题"按钮位置调整

## 概要
将 QuizPage 的"返回上一题"按钮从底部进度条旁移到选项卡片下方，居中显示，使用浅灰色下划线文字样式。

## 当前状态
- [QuizPage.tsx](src/pages/QuizPage.tsx) 底部进度条区域（L143-L171）有一个绝对定位的灰色箭头按钮
- 需要改为选项下方的居中文字链接

## 改动

### 文件：`src/pages/QuizPage.tsx`

**1. 删除底部进度条的箭头按钮**（L143-L171）
- 移除 `relative` 外层 div 和内部的 `motion.button` 箭头
- 恢复为简单的居中进度条 div

**2. 在选项区域下方新增文字链接**
- 在 `</div>` （options 的 flex-col 容器关闭后）插入：
  - 条件渲染：`currentIndex > 0` 时显示
  - `flex justify-center` 居中
  - 样式：`text-zinc-500 underline underline-offset-2 text-sm hover:text-zinc-400 transition-colors`
  - 文字：`← 返回上一题`
  - 点击调用 `handleGoBack`

## 验证
1. `npm run build` 通过
2. 第一题不显示链接
3. 第二题起，选项下方居中显示浅灰色下划线链接
4. 点击回退功能正常，答案回显正确
